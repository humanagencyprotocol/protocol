# Working documents — design docs with open work (doc/) and throwaway scratch
# (temp/, further down). Never tracked, so nothing here may be the ONLY record
# of a decision; see CLAUDE.md → Conventions → "Where a document goes".
# The old root docs/ folder was merged into doc/ on 2026-09-07.
doc/

# Generated: sync-content.js rebuilds this from content/<version>/ on every
# `npm run build` (which CI runs), so the source of truth is content/, not here.
# This used to be covered accidentally by a bare `docs/` rule — kept explicit
# so deleting that rule doesn't start tracking six generated files.
website/src/content/docs/

# Claude AI folder (excluded)
.claude/
CLAUDE.md

# SDK (separate repository)
sdk/
nmd/

# Demo repos (separate repositories)
demo-agent/
demo-deploy/
demo-hap-deploy-gate/
demo-sp/
suveren-gateway/
hap-core/

# Authority Server / Suveren AS (separate repository)
suveren-as/

# Profiles (separate repository)
hap-profiles/

# CRM MCP server (separate repository)
hap-crm-mcp/

# Records MCP server (separate repository)
hap-records-mcp/

# LinkedIn MCP server (separate repository)
hap-linkedin-mcp/

# Google Calendar MCP server (separate repository)
hap-googlecalendar-mcp/

# E2E tests (separate repository)
hap-e2e/
hap-deploy-mcp/

# Local image assets (not tracked in umbrella repo)
images/

# Misc
style-guide.html

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Logs
*.log

# Environment variables
.env
.env.local
!.env.example

prompts.md

# MCP config (local)
.mcp.json

# scratch/session working files — never tracked
temp/
